# Never commit credentials
.env

# Node modules
node_modules/

# Logs
logs/
*.log

# Backtest candle data — fetched/generated locally, can get large
backtest/data/*.json
backtest/results/*.csv

# Personal risk settings — your real dashboard numbers, never commit
backtest/my-risk-settings.js
